{
  "name": "dufc-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply dufc-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply dufc-db --local",
    "migrate:sheets": "node scripts/migrate-from-sheets.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
