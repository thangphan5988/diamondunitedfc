DUFC latest-match UI test build

Run local:
1) unzip folder
2) cd diamondunitedfc
3) python3 -m http.server 8080
4) open http://localhost:8080

Changed files:
- css/latest-match.css
- js/latest-match.js
- index.html (cache bust only)

Focus:
- latest match cap result layout
- bigger pitch on desktop
- bench list moved to right/below responsive
- internal latest match uses wider 2-column layout
